
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Posts</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <link href="/style.css" rel="stylesheet"> <!-- Link to the CSS file -->
    
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">My Website</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
                    <li class="nav-item"><a class="nav-link" href="/posts">Posts</a></li>
                    <li class="nav-item"><a class="nav-link" href="/specific/:index.js">randompost</a></li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="container">
        <h1>Posts</h1>
        <% for(let i = 0;i<posttemplate.length;i++){ %>
            <div class="post">
                <h2><%=posttemplate[i].title %></h2>
                <img src="<%=posttemplate[i].imageUrl %>" alt="Post 3 Image">
                <p><%=posttemplate[i].date %></p>
                <p><%=posttemplate[i].text%></p>
                <a href="<%=posttemplate[i].path%>">Read more</a>
            </div>
    
            
       <% } %>
    </div>
</body>
</html>
